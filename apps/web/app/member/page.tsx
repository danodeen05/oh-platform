import MemberDashboard from "./member-dashboard";

export default function MemberPage() {
  return (
    <main
      style={{
        minHeight: "100vh",
        background: "#E5E5E5",
        padding: 0,
      }}
    >
      <MemberDashboard />
    </main>
  );
}
