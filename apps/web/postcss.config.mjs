// Tailwind v4 for the interactive business plan. Preflight is NOT imported
// (see app/globals.css), so the marketing site's inline styles are untouched.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
